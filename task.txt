Resume the multi-pass repository audit from a prior session using the
`improve` skill. This is a continuation, not a new task.

Ground truth files already in this repo:
- docs/audit-handoff.md (detailed technical handoff)
- docs/session-handoff.md (session summary)
- docs/audit/raw-findings.json (the complete structured findings output
  from the prior run — 46 findings across 6 fully-completed categories)

Step 1: Read docs/audit/raw-findings.json. Treat the findings for these
six categories as already complete — do not re-run them, do not
re-analyze the underlying source files for them:
  correctness (11 findings), performance (6), tests (6),
  architecture (8), deps (5), dx-docs (10)

Step 2: Two categories never completed due to a provider usage limit
(not a safety issue) — run these fresh using the improve skill exactly
as the other six were run:
  ux, direction

Step 3: The security category's dedicated review sub-agent was blocked
by a platform safety classifier and produced zero findings. Do not
re-invoke that same category sub-agent again this run.

Instead, settle only the two specific open items already identified by
direct inline file reading (not a security-review persona):
- Read scripts/migrations/018_migration_recursion_audit_fixup.sql and
  both 019_*.sql files in full.
- Determine whether the operator-tier RPCs in
  scripts/migrations/003_admin_rpcs.sql
  (rpc_get_or_create_draft_voucher, rpc_upsert_voucher_line,
  rpc_create_inventory_txn) have a farm-scope guard, and whether the
  profiles_update_self policy from migration 017 restricts which
  columns a user can update.

Report these two items as either CONFIRMED or RESOLVED based on what
those files actually show. Do not fabricate a finding, and do not
attempt any other security-category work in this run — mark the rest of
that category as deferred, to be completed later as its own narrowly
scoped review outside the `improve` skill's general workflow.

Step 4: Once ux, direction, and the two settled items above are done,
proceed to the improve skill's next phase: present the full consolidated,
leverage-ranked findings table (all 46+ findings plus whatever ux/
direction contribute) for my review and selection.

Do not begin writing implementation plans under plans/ until I have
selected which findings to act on.