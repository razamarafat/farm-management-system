{
  "name": "@morvarid/export-api",
  "version": "0.1.0",
  "private": true,
  "description": "Server-side XLSX export API for Morvarid-Farm reports (Fastify + ExcelJS). Authenticates via Supabase JWT, enforces RBAC, queries the existing reporting_* RPCs, returns styled .xlsx blobs. Never embeds secrets in the SPA bundle.",
  "type": "module",
  "main": "server.mjs",
  "engines": {
    "node": ">=22.12.0 <23"
  },
  "scripts": {
    "start": "node server.mjs",
    "dev": "node --watch server.mjs",
    "test:smoke": "node smoke-test.mjs",
    "test:template": "node template-test.mjs",
    "test:contracts": "node contracts-test.mjs",
    "test:reconciliation": "node reconciliation-test.mjs",
    "test:perf": "node perf-budget.mjs",
    "test:all": "npm run test:contracts && npm run test:template"
  },
  "dependencies": {
    "@fastify/cors": "^9.0.1",
    "@supabase/supabase-js": "^2.95.3",
    "exceljs": "^4.4.0",
    "fastify": "^4.28.1"
  }
}
