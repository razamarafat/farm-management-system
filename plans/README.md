# Implementation Plans

Generated by the improve skill on 2026-07-20 (advisor session, commit
`f3395fe`, dirty working tree — plans reference the working tree state).
Scope of this batch: the UX audit findings (UX-01…UX-14) and direction
findings (DIRECTION-01…06) from the 2026-07-19/20 audit. Execute in the order
below unless dependencies say otherwise. Each executor: read the plan fully
before starting, honor its STOP conditions; the reviewer maintains this index.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Route authenticated users to their role dashboard | P1 | S | — | TODO |
| 002  | Local-date defaults + hash-safe voucher link | P1 | S | — | DONE (commit `cb1adf5`, branch `worktree-agent-a43e74edb1a0475be`, unmerged — reviewed & approved 2026-08-04) |
| 003  | Sidebar navigation + reorder-page discoverability | P1 | M | — | TODO |
| 004  | ConfirmDialog consolidation + Modal semantics | P2 | M | — | TODO |
| 005  | Accessible form kit + NumericInput (Persian digits) | P2 | M | — | TODO |
| 006  | Admin activity-log viewer (/admin/logs) | P2 | M | 003 | TODO |
| 007  | Connectivity banner + honest empty/loading/disabled states | P3 | M | — (sequence after 001–006) | TODO |
| 008  | Dashboard reorder-alert badge | P3 | S | 003 | DONE (commit `77e7485`, direct execution — reviewed & approved 2026-08-09) |
| 009  | Direction decision briefs (sales / offline / import / production) | P3 | S | — | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale).

## Dependency notes

- 006 and 008 assume 003's manifest-driven dashboards (they edit the same
  files; landing them after 003 avoids conflicting diffs).
- 007 edits files plans 002–005 also touch — sequencing it last in the code
  series avoids merge friction; there is no logical dependency.
- All plans share one advisor branch and rebuild `dist/index.html` per commit
  (AGENTS.md RULE 2).
- Baselines at f3395fe the executor must not regress: `npx tsc --noEmit`
  exit 0; `npm run lint` **exit 1** with exactly 23 problems (7 errors, 16
  warnings) — errors are 4× no-explicit-any (FarmItemsPanel 109/135, UserForm
  57, InventoryItemHistoryPage 214), 2× prefer-const (useDailySheet 226,
  useInventory 184), 1× no-useless-escape (persianNumbers 32). eslint exits
  non-zero on any error, so "lint exits 0" was never achievable — the real
  gate is **no new problems**, verified by comparing counts before/after.
  `npm run build` exit 0 (~1.24 MB dist/index.html). `npm test` is red at
  HEAD and is NOT a gate for these plans.

## Findings → plans mapping

| Finding | Disposition |
|---------|-------------|
| UX-01 entry routing | plan 001 |
| UX-02 UTC date defaults | plan 002 |
| UX-03 hash-broken voucher link | plan 002 |
| UX-04 orphaned reorder page | plan 003 |
| UX-05 empty sidebar nav | plan 003 |
| UX-06 confirmation inconsistency | plan 004 |
| UX-08 dialog semantics | plan 004 (semantics + initial focus; full Tab-trap deferred) |
| UX-09 form-control accessibility | plan 005 (kit-level; SearchableSelect combobox deferred) |
| UX-07 Persian-digit numeric input | plan 005 (kit + daily sheet; remaining forms follow-up) |
| UX-11 selector load/error affordances | plan 007 |
| UX-12 dead Excel button / unexplained disabled tiles | plan 007 |
| UX-13 unmounted OfflineBanner | plan 007 |
| UX-14 toast corner | plan 007 (position only) |
| DIRECTION-02/05 activity-log viewer | plan 006 |
| DIRECTION-03 reorder alerting (phase 1) | plan 008 |
| DIRECTION-01 sales entry | plan 009 (decision brief — product call first) |
| DIRECTION-04 offline stack fate | plan 009 (decision brief) |
| DIRECTION-05/06 Excel import, production domain | plan 009 (decision briefs) |

## Findings considered and rejected / deferred (so nobody re-audits them)

- **UX-10 (sub-44px touch targets)**: deferred — MED confidence, class-derived
  without device verification; sizing changes to the daily sheet's dense table
  deserve a visual pass on a real device first. Revisit after plan 005 lands.
- **UX-14 (formatter consolidation)**: only the toast corner is planned; the
  `toPersianNumbers`/`toPersianDigits` duplicate and `toLocaleString()` locale
  pinning span 100+ call sites — a mechanical PR on its own, not worth
  bundling into behavior-changing plans.
- **DIRECTION-01/04/05/06 as build plans**: intentionally NOT built this
  session — each needs a maintainer decision first (plan 009 writes the
  briefs). Building the sales screen or wiring the offline queue before those
  decisions would be advisor overreach.
