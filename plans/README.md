# Implementation Plans

Generated by the improve skill across three advisor sessions (the third also
executed and reviewed the plans):

- **Batch A (2026-07-20, commit `f3395fe`)**: UX audit findings (UX-01…UX-14)
  and direction findings (DIRECTION-01…06) → plans 001–009.
- **Batch B (2026-08-29, commit `a9257dd`)**: deps/CI/test-floor follow-ups →
  plans 010–013 (written but never indexed until the 2026-09-07 session).
- **Batch C (2026-09-07, commit `a9257dd`)**: `deep` audit across all nine
  categories (7 parallel subagents + direct deps pass; the deps subagent was
  cancelled and covered directly) → plans 014–018. Non-interactive default:
  top 5 vetted findings by leverage; findings 6–12 queued for a next batch
  (see "Queued (next batch)" below).

Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions; the reviewer
maintains this index.

> **Tree-state note (2026-09-07, post-execution)**: HEAD is `a9257dd` but the
> working tree is DIRTY (`M package.json, package-lock.json, dist/index.html,
> src/pages/LoginPage.tsx, src/utils/constants.ts`; untracked proxy-tooling
> files + `plans/`). All execution happened in isolated worktrees
> (`C:\Users\Reza\AppData\Local\Temp\opencode\mf-adv-NNN`) — the live tree's
> only change from this session is this index. The dirty `express` hunk in
> `package.json`/`package-lock.json` (a proxy-tooling install artifact) should
> be dropped before merging; `Simple-Qwen-Proxy.js` is ignored by 011's rules.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Route authenticated users to their role dashboard | P1 | S | — | DONE — MERGED to main 2026-09-07 (commit `ed81a7a`; re-verified post-merge: tsc 0, lint 14w/0e, build 0) |
| 002 | Local-date defaults + hash-safe voucher link | P1 | S | — | DONE (commit `cb1adf5`, branch `worktree-agent-a43e74edb1a0475be`, unmerged — reviewed & approved 2026-08-04) |
| 003 | Sidebar navigation + reorder-page discoverability | P1 | M | — | DONE (landed directly in main — manifest wired in Sidebar + 3 dashboards, reorder link in InventoryPage:385; verified 2026-09-07) |
| 004 | ConfirmDialog consolidation + Modal semantics | P2 | M | — | DONE (landed directly — ConfirmDialog in 8 consumers, zero native confirm(, Modal role/aria; verified 2026-09-07) |
| 005 | Accessible form kit + NumericInput (Persian digits) | P2 | M | — | DONE (landed directly — Input htmlFor/aria, Toggle switch, Spinner status, NumericInput in DailySheetTable; verified 2026-09-07) |
| 006 | Admin activity-log viewer (/admin/logs) | P2 | M | 003 | DONE (landed directly — AdminLogsPage + route + useActivityLogs + dashboard link; verified 2026-09-07) |
| 007 | Connectivity banner + honest empty/loading/disabled states | P3 | M | — (sequence after 001–006) | DONE (landed directly — OfflineBanner mounted in AppLayout, zero legacy fetches, Toaster top-right; verified 2026-09-07) |
| 008 | Dashboard reorder-alert badge | P3 | S | 003 | DONE (commit `77e7485`, direct execution — reviewed & approved 2026-08-09) |
| 009 | Direction decision briefs (sales / offline / import / production) | P3 | S | — | DONE — MERGED to main 2026-09-07 (commit `c5017d3`; maintainer decisions on the four briefs remain open) |
| 010 | Remove the unused `express` dependency and land a clean lockfile | P1 | S | — | REJECTED (obsolete at HEAD — verified 2026-09-07: `git show HEAD:package.json` and HEAD lockfile contain no express; the express hunk exists only in the dirty working tree as a side effect of installing deps for the untracked proxy tooling. User action: drop that hunk. Recurrence prevented by 011. 012/013's dependency on 010 is satisfied vacuously.) |
| 011 | Make it impossible to commit the untracked local proxy tooling, and flag the exposed token for rotation | P1 | S | — | DONE (worktree `advisor/011-ignore-local-proxy-tooling`, commit `6f6038e` — reviewed & approved 2026-09-07; MERGED to main 2026-09-07; operator must still rotate token (see below)) |
| 012 | Repair the CI workflow (DB-job gates, export-api deps) | P1 | S | 010 (satisfied vacuously — see 010) | DONE — MERGED to main 2026-09-07 (commit `07ff4f9`; re-verified post-merge: export-api tests green under repaired CI) |
| 013 | Establish a unit-test floor with vitest (persianNumbers, jalaliDate, rpcError) | P1 | M | 010 (vacuous), 012 | DONE — MERGED to main 2026-09-07 (commit `3e05047`; CI unit step verified in merged validate job) |
| 014 | Draft saves: bulk upsert + error check + submit gate | P1 | M | 013 | DONE — MERGED to main 2026-09-07 (commit `17dd8ed`; post-merge: 93/93 unit tests, dist rebuilt in merge-tip) |
| 015 | Harden the BFF auth-admin surface (CORS, PATCH, quiet responses, throttle) | P1 | S | — | DONE — MERGED to main 2026-09-07 (commit `91f1da7` after 1 revision round; node --check 0) |
| 016 | Escape user search text before PostgREST `or()`/`ilike` | P1 | S | 013 | DONE — MERGED to main 2026-09-07 (commit `b2adeb0`; post-merge battery green) |
| 017 | Align env truth across guard, README, and templates | P2 | S | — | DONE — MERGED to main 2026-09-07 (commit `2467c18`) |
| 018 | Close the secret-scanner blind spot over `bff/` | P2 | S | 011 | DONE — MERGED to main 2026-09-07 (commit `263121f`) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale).

## Dependency notes

- 006 and 008 assume 003's manifest-driven dashboards (they edit the same
  files; landing them after 003 avoids conflicting diffs).
- 007 edits files plans 002–005 also touch — sequencing it last in the code
  series avoids merge friction; there is no logical dependency.
- 012 requires 010 (clean lockfile before trusting `npm ci`); 013 requires
  010 + 012 (new CI test step needs a working pipeline). 010 is REJECTED-
  obsolete at HEAD — its dependency edges are satisfied vacuously (verified:
  no express in `a9257dd:package.json` / HEAD lockfile).
- 014 and 016 require 013 (new tests use its vitest setup — no second runner).
- 018 requires 011 (widen the scanner only after the proxy-tooling ignore set
  is settled, or new coverage flags files 011 handles).
- All source-touching plans verified with `npx tsc --noEmit` exit 0 + lint
  problem-count unchanged (14 warnings / 0 errors at `a9257dd`) + unit suite
  green (81 @013, 84 @014, 90 @016) + build exit 0. Stacked branches restored
  `dist/`+`tsconfig.tsbuildinfo` after verification; rebuild once at merge.

## Merge history (completed 2026-09-07)

All ten executed branches were merged into `main` in dependency order
(012 → 011 → 018 → 015 → 017 → 001 → 009 → 013 → 014 → 016) with **zero
conflicts** (validated by a full integration branch first):

```
main → advisor/012-repair-ci-workflow (07ff4f9)          [ci only]
     → advisor/011-ignore-local-proxy-tooling (6f6038e)  [gitignore + scanner]
     → advisor/018-scanner-bff-coverage (263121f, stacked on 011 — merge via 018's tip)
     → advisor/015-bff-auth-admin-hardening (91f1da7)    [bff only]
     → advisor/017-env-truth (2467c18)                   [docs/env templates]
     → advisor/001-entry-routing-role-home (ed81a7a)     [routes + login + dist]
     → advisor/009-direction-decision-briefs (c5017d3)   [docs only]
     → advisor/013-vitest-unit-test-floor (3e05047, stacked on 012 — merge via 013's tip)
     → advisor/014-draft-save-fidelity (17dd8ed) + advisor/016-postgrest-search-escape (b2adeb0)
       — both stack cleanly on 013; 014↔016 touch disjoint files
```
Conflicts expected only where package-lock.json / dist/index.html are
touched by multiple branches — resolve by taking both code changes, then
`npm ci && npm run build` once and committing the regenerated artifacts.

## Baselines ( refreshed 2026-09-07, commit `a9257dd`, dirty tree — see note )

- `npx tsc --noEmit` → exit 0 expected (was 0 at `a9257dd` per its message).
- `npm run lint` → hooks-focused config; executors must record the
  before/after problem count (the old "23 problems" baseline from `f3395fe`
  predates the eslint-config rewrite — do NOT use it as a gate; "no new
  problems" is).
- `npm test` (`scripts/test-spa-reports.mjs`) is RED at HEAD and NOT a CI gate;
  plans 013/017 own its future, not every plan.
- `npm run check:secrets` goes RED in the LOCAL main tree only because of
  three stale, git-ignored `.claude/worktrees/*` copies of `bff/README.md:46`
  whose "secret" is the 6-char doc placeholder `eyJ...` (root `bff/README.md`
  is already in the scanner's SELF exempt-list; CI checkouts lack these dirs,
  so CI stays green). Verified benign 2026-09-07. Fix belongs in
  `scripts/check-secrets.mjs` (skip paths containing `/worktrees/`) as a
  future executor task — the dirs are other sessions' live worktrees; do not
  delete them.
- Verification commands per repo: `npx tsc --noEmit`, `npm run lint`,
  `npm run build`, `npm run check` (conflicts + env + secrets +
  legacy-admin:strict), export-api `test:contracts` / `test:template`.

## Findings → plans mapping (Batch C, 2026-09-07 deep audit)

| Finding | Disposition |
|---------|-------------|
| Draft-upsert silent loss + void submit gate + N sequential upserts (bug/perf) | plan 014 |
| BFF wildcard CORS + PATCH mass-assignment/weak validation/verbose errors, no throttle (security) | plan 015 |
| Unescaped search interpolation, 5 hooks (bug/security) | plan 016 |
| Env-truth drift: check-env header vs guard, README table/Quick-Start/troubleshooting, root `.env.example` forbidden key, export-api PORT collision (docs/security) | plan 017 |
| Secret scanner skips `bff/` (security) | plan 018 |
| Deps pass: `express` unused (verified zero imports) | already plan 010 — no new plan |

## Queued (next batch — vetted, not yet planned)

- `updateTransaction` falsy-zero `total_price` + unvalidated history edits
  (`src/hooks/useInventory.ts:422-444`) — S, LOW risk.
- Negative-stock enforcement beyond the voucher RPC (transfer-out / adjustment /
  edit / delete) — M, MED risk (likely needs a server-side guard or RPC).
- Unbounded ledger reads feeding balances; 500-cap list with post-limit
  filtering; page-not-reset (`src/lib/offline/reads.ts:55-74`,
  `src/hooks/useInventory.ts:130-185,458-486`) — M.
- Daily-sheet open waterfall (~8 sequential awaits) + reorder badge firing the
  full ABC RPC per dashboard visit — S+M, LOW risk.
- `rpc()` drops the Postgres error code; direct `STATUS_COLORS[v.status]`
  index crashes on unknown voucher status — S.
- Dead `useOfflineSync` queue vs RxDB stack; initial-stock check-then-act race
  — needs plan-009 direction decision first (offline verdict).
- Test gaps: red `npm test` ownership, auth/report boundary tests, stale export
  smoke test (partly covered by plan 013's floor).
- Full `npm audit` readout; line-by-line RLS-vs-JWT matrix; measured perf
  profiles — not covered at depth in this audit (read-only rule: no builds).

## Operator actions (not automatable)

- **Rotate the bearer token hard-coded at `Simple-Qwen-Proxy.js:11`** (local
  LLM proxy tooling, untracked as of plan 011 — worktree commit `6f6038e`
  approved 2026-09-07). It has been readable in the working tree and must be
  considered compromised. Revoke it at the issuing provider and re-issue;
  supply the replacement via an environment variable rather than an `||`
  fallback default. Also remove the truncated-token `console.log` at
  `Simple-Qwen-Proxy.js:13`. Plan 011 only made the file unstageable — it did
  not and cannot rotate the credential.
- **Merge status 2026-09-07**: ten reviewed+approved branches (worktree copies
  under `C:\Users\Reza\AppData\Local\Temp\opencode\mf-adv-*`) — all MERGED
  into `main` (zero conflicts) and pushed to `origin/main`. The live working
  tree's user WIP (v1.1.1 root/bff/constants bumps + login-card theme-toggle
  reposition) was committed first as `chore(release)`; the stray unused
  `express` hunk and root proxy-tooling files were deliberately NOT committed,
  and `dist/index.html` was rebuilt once at the merged tip + verified
  demo-string-clean (AGENTS RULE 2).
- **Still unmerged (operator's choice 2026-09-07)**: plan 002's approved
  branch `worktree-agent-a43e74edb1a0475be` (`cb1adf5` — local-date defaults +
  hash-safe voucher link). Merge-test ran on top of merged main: **src files
  merge clean** (`jalaliDate.ts`, `DailySheetPage`, `InventoryPage`,
  `ReorderPointPage`); only the bundled `dist/index.html` conflicts — resolve
  by `git checkout --theirs`-style accept + one `npm run build`. Left
  untouched per operator choice.
- **First-PR check after 012 lands**: confirm `reconciliation` and `perf-budget`
  jobs run when secrets are configured (no local command can assert this).

## Reconcile notes (2026-09-07 execution session)

- 002 stays DONE-unmerged: branch `worktree-agent-a43e74edb1a0475be`
  (`cb1adf5`) verified present; main still uses the UTC date default, which is
  consistent with unmerged status.
- 008 verified in main history (`77e7485` is an ancestor of HEAD).
- Plan-number collision to know: plan 013 (2026-08-29) refers to a *future*
  "plan 015" that would change `jalaliDate.ts` fallbacks — our Batch-C 015 is
  the BFF-hardening plan instead (that future jalaliDate fix was never planned;
  the `KNOWN DEFECT (plan 015):` test titles still execute correctly as
  strings). Likewise "plan 020" referenced in 011/012 (admin-client migration
  completion) does not exist yet — queued below.
- A `git stash` entry (`!!GitHub_Desktop<main>`) predates this session; left
  untouched.
- Queued (vetted, unplanned): admin-client migration completion incl.
  `supabase-admin.ts` deletion (the "020" scope), `updateTransaction`
  falsy-zero totals, negative-stock enforcement beyond the voucher RPC,
  unbounded ledger reads, daily-sheet waterfall + badge RPC, `rpc()` code
  threading, offline-queue verdict (needs 009 briefs), red-`npm test`
  ownership, full `npm audit`, RLS-vs-JWT matrix.

## Findings considered and rejected (so nobody re-audits them)

- **Batch A carried over**: UX-10 (touch targets — deferred, needs device
  verification); formatter consolidation (separate mechanical PR); building
  DIRECTION-01/04/05/06 before plan 009's decision briefs (advisor overreach).
- **Batch C (2026-09-07)**: transfer/adjustment stock-source gate — REJECTED
  after vetting: `has_initial` (`src/hooks/useDailySheet.ts:263`) already sums
  ALL inbound `qty_in`, so transfers/adjustments count; only the field name
  misleads (rename welcome, no plan). Single-file-vs-`lazy()` bundle split
  (PERF-02) — needs a maintainer deploy decision first, not a build plan.
  God-page splits (Inventory/Purchases/Formula/Reorder) and deploy-doc
  three-way drift (`render.yaml` vs guide vs `bff/README.md`) — real, queued
  above, lower leverage than correctness/security this batch.
